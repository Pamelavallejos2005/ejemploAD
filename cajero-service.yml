server:
  port: ${PORT:8863}
message: Configuracion de Cajero en el Servidor levantado!
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:8089/crudcloud?serverTimezone=UTC
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
cloud:
  config:
    url: http://localhost:8860

  
